
<% provide(:title, 'All users') %>

<h1>Listing Users</h1>


<%= will_paginate %>

<ul class="users">

      <%= render @user_infos %>

</ul>

<%= will_paginate %>

<br>
<%= link_to 'New User info', new_user_info_path %>
